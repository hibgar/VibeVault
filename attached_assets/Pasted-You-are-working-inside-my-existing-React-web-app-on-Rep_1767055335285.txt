You are working inside my existing React web app on Replit. Do NOT rebuild the project. I want simple account sign-up/sign-in with email + password, and per-user cloud saving. Do NOT send any emails (no verification emails, no password reset emails). Users should be able to sign up and immediately use the app.

Use Supabase (Auth + Postgres) and implement the following:

1) Setup
- Install @supabase/supabase-js.
- Create src/lib/supabaseClient.(ts|js) that initializes Supabase using environment variables ONLY (do not hardcode keys).
- If this is a Vite app, read from import.meta.env.VITE_SUPABASE_URL and import.meta.env.VITE_SUPABASE_ANON_KEY.
- Add a small helper to get the current session/user.

2) Auth UI (email + password only)
- Create a clean Auth component/page with:
  - Email input
  - Password input
  - Buttons: “Sign up” and “Sign in”
  - “Sign out” when logged in
- Persist the session and restore it on refresh (use getSession + onAuthStateChange).
- Display friendly error messages.

3) IMPORTANT: No emails
- Configure the app so sign-up does not require email confirmation (users should be able to sign in right away).
- Do not implement password reset, magic links, or any email sending flows.
- Include a short note explaining the exact Supabase dashboard setting I need to toggle to disable email confirmation (do not assume it is already off).

4) Per-user data saving
- Create a table user_data with:
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id),
  data jsonb not null default '{}'::jsonb,
  updated_at timestamptz not null default now()
- Enable Row Level Security (RLS) on user_data.
- Add policies so users can ONLY select/insert/update rows where user_id = auth.uid().

5) App behavior
- After login:
  - Load the current user’s user_data row.
  - If it doesn’t exist, create it with default data.
- Provide functions:
  - loadUserData(): fetches the user’s JSON
  - saveUserData(partialUpdate): merges updates into the JSON and updates updated_at
- Add UI states: loading, saving, error, and a “Saved” indicator.
- Add an example “My Library / Settings” screen that demonstrates editing a few fields and saving.

6) Output requirements
- Print the exact files you added/modified with full code.
- Provide the exact SQL to create the table + RLS policies.
- List the exact environment variables I must add in Replit Secrets.
- Confirm where in the UI the auth screen is wired (route/component) so I can find it quickly.
