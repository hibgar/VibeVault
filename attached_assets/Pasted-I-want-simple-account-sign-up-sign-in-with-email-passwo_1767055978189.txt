I want simple account sign-up/sign-in with email + password, persistent login on the same device, and per-user cloud data saving. Users should be able to sign up and immediately use the app.

Use Supabase (Auth + Postgres) and implement the following:

1) Setup
- Install @supabase/supabase-js.
- Create src/lib/supabaseClient.(ts|js) that initializes Supabase using environment variables ONLY (do not hardcode keys).
- If this is a Vite app, read from import.meta.env.VITE_SUPABASE_URL and import.meta.env.VITE_SUPABASE_ANON_KEY.
- Export helpers to get the current session and user.

2) Auth UI (email + password only)
- Create a clean Auth component/page with:
  - Email input
  - Password input
  - Buttons: “Sign up” and “Sign in”
  - “Sign out” when logged in
- Persist the session so users remain logged in on the same device.
- Restore the session automatically on app load using:
  - supabase.auth.getSession()
  - supabase.auth.onAuthStateChange(...)
- Display friendly loading and error states.

3) IMPORTANT: No emails
- Configure the app so sign-up does NOT require email confirmation (users should be logged in immediately).

4) Per-user data saving
- Create a table user_data with:
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id),
  data jsonb not null default '{}'::jsonb,
  updated_at timestamptz not null default now()
- Enable Row Level Security (RLS) on user_data.
- Add policies so users can ONLY select/insert/update rows where user_id = auth.uid().

5) App behavior
- After login (or session restore):
  - Load the current user’s user_data row.
  - If it does not exist, create it with default data.
- Provide functions:
  - loadUserData(): fetches the user’s JSON data
  - saveUserData(partialUpdate): merges updates into the JSON and updates updated_at
- Add UI states: loading, saving, error, and a visible “Saved” indicator.

6) Code quality
- Keep changes minimal and organized (lib/, hooks/, components/).
- Add clear comments explaining auth persistence and data ownership.
- Do NOT break existing routes/components.

7) Output requirements
- Print the exact files you added or modified with full code.
- Provide the exact SQL statements for creating the table and RLS policies.
- List the exact environment variables I must add in Replit Secrets.
- Clearly state where the Auth component is wired into the app (route or parent component).
